'use strict';

(function($) {
    $(function() {
        console.log('we are ready!!!');
    });

    //function injectCSS() {
    //var path = chrome.extension.getURL('styles/main.css');
    //inject(path);
    //}

    //function inject(path) {
    //$('head').append($('<link>')
    //.attr('rel', 'stylesheet')
    //.attr('type', 'text/css')
    //.attr('href', path));
    //}

})(jQuery);
